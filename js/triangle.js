console.log('connected');
function calculateTriangle(){
   // get width
   const triangleWidth = document.getElementById('triangle-width').value;
   const width = parseFloat(triangleWidth);
   // Get height
   const triangleHeight = document.getElementById('triangle-height').value;
   const height = parseFloat(triangleHeight);

   console.log(height);
}